namespace MyConsoleMapleRPG.UI.Settings
{
    // 여러 화면에서 공통으로 사용하는 콘솔 크기와 좌표값을 모아둔 설정 클래스
    // 화면 배치 변경시 수정 범위를 줄이기 위해 사용.
    internal static class Layout
    {
        internal const int MainWidth = 95;
        internal const int MainHeight = 35;

        internal const int CharacterSelectWidth = 95;
        internal const int CharacterSelectHeight = 42;

        internal const int MainWindowX = 2;
        internal const int MainWindowY = 1;
        internal const int MainWindowWidth = 90;
        internal const int MainWindowHeight = 32;

        internal const int GameMapWindowX = 2;
        internal const int GameMapWindowY = 1;
        internal const int GameMapWindowWidth = 55;
        internal const int GameMapWindowHeight = 32;

        internal const int SidePanelX = 59;
    }
}